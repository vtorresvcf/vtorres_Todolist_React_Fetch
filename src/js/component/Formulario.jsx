import React, { useState } from "react";

const Formulario = ({
  addTarea,
  valueInput,
  setValueInput,
  changeValueInput,
}) => {
  // Registra el envío del formulario

  return <div></div>;
};

export default Formulario;
